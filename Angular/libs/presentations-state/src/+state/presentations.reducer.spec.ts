import { presentationsReducer } from './presentations.reducer';
import { presentationsInitialState } from './presentations.init';
//import { Presentations } from './presentations.interfaces';
import { LoadSuccess } from './presentations.actions';

describe('presentationsReducer', () => {
  it('should work', () => {
//    const state: Presentations = {};
//    const action: LoadSuccess = { type: 'DATA_LOADED', payload: {} };
//    const actual = presentationsReducer(state, action);
//    expect(actual).toEqual({});
  });
});
