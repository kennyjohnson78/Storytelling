import { boxesReducer } from './boxes.reducer';
import { boxesInitialState } from './boxes.init';
//import { Boxes } from './boxes.interfaces';
import { LoadSuccess } from './boxes.actions';

describe('boxesReducer', () => {
  it('should work', () => {
//    const state: Boxes = {};
//    const action: LoadSuccess = { type: 'DATA_LOADED', payload: {} };
//    const actual = boxesReducer(state, action);
//    expect(actual).toEqual({});
  });
});
